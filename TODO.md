# TODO

- [x] Identify 404 cause (missing entry script `js/app.js` on GitHub Pages)
- [x] Inspect existing frontend module structure under `js/`
- [x] Create missing entrypoint `js/app.js` to bootstrap the app and hide the spinner
- [ ] Verify locally that `index.html` loads without 404 and renders the hero
- [ ] (Optional) Ensure GitHub repo pushes include `js/app.js`

